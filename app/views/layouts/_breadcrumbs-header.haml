- if params[:controller] == 'products' && params[:action] == 'show'
  .breadcrumbs-header-contener
    .breadcrumbs-header
      - breadcrumb "products_category_#{@product.category.depth}".to_sym
      = breadcrumbs separator: " &nbsp;&nbsp;&#12297;&nbsp; "
